<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vue.data.demo.vuerequest.mapper.UserMapper">

    <select id="login" parameterType="user" resultType="User">
        select * from user where name=#{name} and password=#{password}
    </select>
    <insert id="addUser" parameterType="User">
        insert into user value (null,#{name},#{password},#{age},null,null)
    </insert>
    <delete id="delUserById" parameterType="Integer">
        delete from user where id=#{id}
    </delete>
    <select id="getUserList" parameterType="Map" resultType="User">
        select * from user
        <where>
            <if test="name != null and name !=''">
                and name like '%${name}%'
            </if>
        </where>
        limit #{pageStart},#{pageSize}
    </select>
    <select id="getUserList1" parameterType="user" resultType="User">
        select * from user
    </select>
    <select id="getUserByName" parameterType="User" resultType="user">
        select * from user
        <where>
            <if test="name != null and name !=''">
                and name like '%${name}%'
            </if>
        </where>
    </select>
    <select id="getUserById1" parameterType="User" resultType="user">
        select * from user where id=#{id}
    </select>
    <update id="updateUser" parameterType="user">
        update user set name=#{name},age=#{age},gender=#{gender},headPath=#{headPath} where id=#{id}
    </update>
    <update id="updatePwd" parameterType="user">
        update user set password=#{password} where id=#{id}
    </update>
    <update id="updateName" parameterType="user">
        update user set name=#{name} where id=#{id}
    </update>
    <delete id="deleteUser" parameterType="list">
        delete from user where id in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </delete>
    <select id="selectUserCount" resultType="integer">
        select count(*) from user
    </select>
</mapper>
