<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.vue.data.demo.vuerequest.mapper.ProductMapper">
    <select id="getProductList" parameterType="Map" resultType="Product">
        select * from product
        <where>
            <if test="productName != null and productName !=''">
                and productName like '%${productName}%'
            </if>
        </where>
        limit #{pageStart},#{pageSize}
    </select>
    <select id="selectProCount" resultType="integer">
        select count(*) from product
    </select>
    <select id="getProductById" parameterType="Product" resultType="product">
        select * from product where id=#{id}
    </select>
</mapper>
